/**
 * Compatibility re-export: dashboard tab lives under pages/projects/dashBoard.
 * Keeps older imports and merge/apply tooling that expect this path working.
 */
export { default } from '../pages/projects/dashBoard/ProjectDashboardTab';
